// Here is where we define the types for our application

// The type containing the data for tables
type TableData = { 
    label: string,
    value: number,
    prevPos?: number,
    prevValue?: number
}[];
  
